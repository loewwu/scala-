1. 类是对象的抽象，而对象是类的具体实例。
类是抽象的，不占用内存，而对象是具体的，占用存储空间。类是用于创建对象的蓝图，它是一个定义包括在特定类型的对象中的方法和变量的软件模板。

2. 类定义了域与方法，构建某个类的val和var称为创建实例或创建对象；
例如：r1和r2则是类Range的两个实例或对象，每个对象在内存中都有不同的地方来存储，r1和r2代表范围不同；
val r1 = Range(1,10)
val r2 = Range(5,7)

3. 类中可以定义很多方法，也即很多操作，通过生成类的对象，可以调用类库中定义的方法，即执行了某个操作；

4. 创建类
